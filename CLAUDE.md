# sizucu compass ─ 経営の羅針盤（sizucu-compass／旧称 薬局管理ツール）

## 全体CLAUDE.mdから移設（2026-09-03）

> 以下は `H:\マイドライブ\CLAUDE.md` の一覧表に書かれていた内容を、always-loaded を軽くするためこちらへ移した（2026-09-03）。原本のバックアップは `H:\マイドライブ\_旧版アーカイブ\CLAUDE_20260903.md`。

### 公開サイト一覧からの詳細（sizucu compass ─ 経営の羅針盤（sizucu-compass／旧称 薬局管理ツール））

（**合言葉1111だけで入る**。**2026-08-07に「sizucu compass（経営の羅針盤）」へ改名し、会社HP sizucu.co.jp のデザイン（名刺パレット：ネイビー#3d4e5e×ペーパー#fbfbf8／Noto Serif JP＋Zen Kaku Gothic New＋Cormorant Garamond）に全面統一**。**同日さらにリポジトリ名も `hojokin-kanri`→`sizucu-compass` へ統一**＝公開URLは `/sizucu-compass/` に変わり、**旧URL https://ph2144tt0609-cmyk.github.io/hojokin-kanri/ は404（GitHubはPagesのリダイレクトを作らないのでブックマークの貼り替えが必要／gitのremoteは旧URLでも自動転送される）**。ローカルも `C:\dev\sizucu-compass` へ改名し、`vite.config.ts` の `base` を `/sizucu-compass/`・`package.json`／`.claude\launch.json` の name も変更した（**baseを直さないとassetが404になる**）。2026-08-04に経営ダッシュボードを統合し[経営ダッシュボード][補助金管理][ベースアップ評価料]の3タブに。データはAES-256-GCMで暗号化して Supabase `app_state` に保存＝サーバには暗号文しか置かない。ログイン方式は廃止・旧データは画面の「以前のデータを取り込む」で引っ越す。**2026-08-06に処方箋枚数の二重入力を解消**＝処方箋枚数を入れる場所は経営ダッシュボードタブの1か所だけ。ベースアップ評価料タブは経営ダッシュボードの `total` を自動参照し〔緑ヶ丘＋鷹匠の合算〕、例外月だけ「手入力にする」で上書きできる。JSON取込ボタンは廃止。同日**ベースアップ評価料を法人1本に一本化**〔薬局別タブ廃止・職員/係数/充当判定/年度サマリー/CSVは法人合算、収入の内訳のみ月次明細で薬局別〕。移行前のv3データは `app_state` の `baseup-backup-v3` に退避済み。**2026-08-06に最上位タブを4つに並列化**＝[経営ダッシュボード][調剤基本料 判定][ベースアップ評価料][補助金管理]。調剤基本料 判定は経営ダッシュボードの中の切替だったものを引き上げた〔`DashboardTab` は `view` プロップで表示切替・データは共通〕。同日**ベースアップ評価料に賃金台帳の取り込みを追加**＝給与ソフトの賃金台帳CSV（1人1ファイル・列＝○月度・Shift-JIS・見出しに改行入り）をブラウザ内で解析し、月別の実残業時間／ベースアップ手当／事業主負担額を取り込む。台帳のある月はその実績、無い月は職員表の固定値で計算（それまで残業時間が固定値だったため増えた残業代が毎月同額になっていた）。増加分法定福利費は「便宜計算16.5%」と「台帳の実額＝事業主負担÷支給合計」を選べる。台帳の原本は送信せずブラウザ内で処理・取り込んだ数字は暗号化保存。サンプル台帳は `しずく内部\34_県補助金\_賃金データ`）

---

## 見た目（2026-09-03 にマネーフォワード寄りへ刷新）★上の「会社HPのデザインに全面統一」より新しい★

**「全体的に淡くて目が疲れる／マネーフォワードのUIに寄せたい」というユーザー指示で、2026-09-03 に見た目を作り替えた。**
上の 2026-08-07 の記述（会社HPの名刺パレット・明朝見出し・角は立てる）は**この日で終了**。戻さないこと。

- **トークンの正本は `src/index.css` の `:root`**（ここを直せば全タブに効く）
  - 地＝はっきりしたグレー `--bg:#eef1f4`（クリーム #fbfbf8 ＋薄青の放射グラデは廃止。**白いパネルの輪郭がぼやけて「淡い」の主因だった**）
  - 文字を濃く `--text:#1b2836` / `--muted:#46586a`。ステータス色（赤・緑・琥珀）も**白地で4.5:1以上**まで濃くしてある
  - 罫線を見える濃さに `--border:#d2d9e0`（旧 #dde4e8）。角丸は `--radius:6px` / `--radius-sm:4px`（旧 2px）
  - **見出しの明朝＋広い字間をやめ、全面ゴシック**＝`--head`（= `--sans`）を使う。明朝は `--mincho` として残してあるが**ブランド表記だけ**
  - **押せるもの（ボタン・タブ・チップ・focus）は青 `--accent:#1a6bb0` ひと色**。ネイビーは構造（ヘッダー・合計行）専用
- **⚠️ `--accent` はグローバルの「操作の青」**。サマリーカードが1枚ずつ持つ色（左の縦線・欧文ラベル）は
  同名衝突を避けて **`--bar`** に改名済み（`.sum-count/.sum-applied/.sum-paid/.sum-unpaid`）。`--accent` に戻さない
- 構造＝ヘッダー＋タブを**画面幅いっぱいの帯 `.appbar`** に出し、中身だけ `.bar-inner` で1200pxに揃える（App.tsx）。
  ヘッダーはネイビーのベタ塗り＋白文字・**ロゴは `filter: brightness(0) invert(1)` で白抜き**。
  タブは面を塗るのをやめ**選択中を青い下線**で示す（720px以下は折り返さず横スクロール）
- ダッシュボード（`src/dashboard/*.jsx`）は色を**インラインの `C` オブジェクト**で持つ。`index.css` と同値に揃えてあるので
  **片方だけ直すと色がズレる**。`C.border` は `rgba(61,78,94,.10)` から実線グレーに変えた（罫線が見えるように）
- 丸ピル＋斜めグラデ＋光る影の切替UIは全部やめた（四角いボタン＋青ベタ＋影は `0 1px 2px` まで）。
  KPIカードのパステル塗りもやめ、**白い面＋左3pxの色線＋濃い数字**にしてある
- `src/Baseup.css` のローカル変数（`--teal` 等）は**共通トークンを参照する形**に付け替えた。ここで色を直接持たない。
  **⚠️ `.baseup` の中で `--muted` を再宣言しない**（自己参照は循環で無効になる。`:root` から継承させる）

**検証のこつ**：内蔵ブラウザペインは**スクロール位置が0のときだけ**スクリーンショットが正しく出る（スクロール後は
sticky ヘッダーが途中に描かれ上が空白になる＝合成の不具合で、レイアウト自体は正常）。下の方を見たいときは
`getBoundingClientRect` で数値を見るか、タブを切り替えて先頭から撮る。

---

## 1ページめは「早見表」（2026-09-04 追加）

**「最初から各論すぎる／1ページめは全体像がぱっとつかめる早見表がいい」というユーザー指示で、
タブを5つにして先頭に早見表を置いた**（既定表示も早見表）。
タブの順＝[早見表][経営ダッシュボード][調剤基本料 判定][ベースアップ評価料][補助金管理]。

- 実体は `src/dashboard/OverviewTab.jsx`。中身は上から
  ①法人合計の直近月（売上・処方箋枚数・技術料率・集中率＋前月比）
  ②直近12か月の棒＋線（売上／処方箋枚数）
  ③**気になるところ**＝重い順に最大6件・押すとその道具のタブへ飛ぶ
  ④4つの道具の現在地（結論だけ）
  ⑤薬局別の一覧表
- **⚠️ このページは読み取り専用**。`cloudSave` を呼ばない（`migrateBaseup` は変換だけして保存しない）。
  早見表から書くと、各タブが持っている「新しい方を採る」判定と喧嘩する
- **⚠️ 申請済みの補助金に「申請期限」の警告を出さない**（もう打てる手がなく、期限超過の案件が
  ずっと並ぶだけの雑音になる。2026-09-04 に実データで4件出て気づいた）。期限を見るのは未申請のものだけ
- **⚠️ 数字の計算はここに書かない**。同じ数字を早見表と各タブが出すので、計算は共有モジュールに置く：
  - `src/dashboard/pharmModel.js` … override の読み書き／seed との重ね合わせ／派生指標（集中率・技術料率）／
    法人合算（`buildCorpRows`＝両薬局そろった月だけ）／**年度をまたぐ月次の並び `seriesOf`**（前月比は
    9月→10月の年度の切れ目をまたぐので、年度ごとの配列ではなくこの一本で見る）
  - `src/dashboard/basicFeeCalc.js` … 1,800回・85%の2条件判定（`judgeBasicFee`）
  - `src/dashboard/theme.js` … 色と書体（DashboardTab / BasicFee / OverviewTab で共有。
    ※index.css の :root と同値なので、色を変えるときは両方直す）
  - `src/baseupCalc.ts` … `buildBaseupRows` / `sumBaseupRows` / `baseupSummary`（充当率の正本）
- **⚠️ `.jsx` は tsc で型検査されない**。2026-09-04 の切り出しで、消した変数の参照（`seed`・`sumEx`）が
  `npm run build` を素通りして実行時エラーになった。`eslint.config.js` に js/jsx 用の設定を追加して
  `no-undef` が効くようにしたので、**jsx を触ったら `npx eslint src` を通す**（build だけでは足りない）

### 早見表の「入っていないデータ」（2026-09-04 追加）

「何が足りていないのか」も早見表で分かるように、`gaps` の表を追加した。
薬局ごとの月次／片方だけ入力の月（法人合算に入らない）／法人の人件費／調剤基本料の判定期間／
ベースアップの受付回数・賃金台帳／補助金の金額・期限 を、足りないものを上・そろっているものを下に並べる。

- **⚠️ まだ来ていない月を「未入力」と数えない**。判定は **`lastClosedYm()`＝先月まで**で見る
  （今月は数字が出そろわない）。調剤基本料の判定期間（12か月）とベースアップの月の枠は
  **先の月まで用意されている**ので、これを入れないと「12か月中4か月・未入力8か月」のような
  嘘の警告が出る（2026-09-04 に実際に出した）
- **⚠️ 開局が遅い薬局の開局前を「片落ち」と言わない**。片方だけ入力の月は
  遅く始まった薬局の最初の月（`commonFrom`）以降だけ数える
- そろっている項目もあえて ✓ で残す（確認済みだと分かる＝「出ていない＝見落とし」と区別できる）
